/**
 * Default settings seed — REMOVED.
 *
 * Application defaults now live as code constants in src/common/config.ts.
 * The database stores only explicit user overrides.
 * See migration 037 which cleaned up previously-seeded rows.
 */
